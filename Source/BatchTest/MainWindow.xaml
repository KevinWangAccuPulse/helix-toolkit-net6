<Window
    x:Class="BatchedMeshDemo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hx="http://helix-toolkit.org/wpf/SharpDX"
    xmlns:local="clr-namespace:BatchedMeshDemo"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="BatchedMesh Demo"
    Width="800"
    Height="600"
    mc:Ignorable="d">

    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>

    <Grid>
        <hx:Viewport3DX
            Camera="{Binding Camera}"
            EffectsManager="{Binding EffectsManager}"
            BackgroundColor="Black"
            ShowCoordinateSystem="True"
            ShowViewCube="True">

            <hx:AmbientLight3D Color="#404040"/>
            <hx:DirectionalLight3D Direction="-100,-200,-150" Color="White"/>

            <hx:BatchedMeshGeometryModel3D
                BatchedGeometries="{Binding BatchedMeshes}"
                BatchedMaterials="{Binding BatchedMaterials}"
                CullMode="Back"
                IsThrowingShadow="True" />
        </hx:Viewport3DX>
    </Grid>
</Window>